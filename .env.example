# Slack notifications — use bot token (preferred) or webhook
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_CHANNEL_ID=C0AC2NEL32R
# Fallback: Slack webhook (used if no bot token)
SLACK_WEBHOOK_URL=

# SerpAPI key (optional — enables higher-quality Google search results)
# Without this, the scraper uses direct Google search parsing
SERPAPI_KEY=

# Storage path for results database
STORAGE_PATH=./data/jobs.json

# Scoring thresholds
MIN_SCORE=40

# Rate limiting (seconds between requests)
REQUEST_DELAY=2.0

# Enable/disable individual scrapers (1=on, 0=off)
SCRAPER_LINKEDIN=1
SCRAPER_INDEED=1
SCRAPER_ABOVEBOARD=1
SCRAPER_WELLFOUND=1
